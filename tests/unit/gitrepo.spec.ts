const {describe, it, expect} = require('@jest/globals')

describe('github api tests', () => {
    it('should fetch valid response', () => {
    });

    it('should fail with non github url', () => {
        
    });
    it('should fail with malformed github url', () => {

    });
});

describe('invalid url tests', () => {
    it('check root url', () => {

    })
})