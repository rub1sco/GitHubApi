import { describe, it, jest } from '@jest/globals'

describe('Integration tests', () => {
  it('should fetch \'/ GET endpoint and return {}', () => {

  })

  it('should fetch \'/git POST endpoint with GitRepo www.github.com/nodejs/node', () => {

  })
})
